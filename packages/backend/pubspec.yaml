name: visual_app_builder_backend
description: Backend server for Visual App Builder PWA
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  shelf_web_socket: ^1.0.4
  web_socket_channel: ^2.4.0
  path: ^1.8.3
  archive: ^3.4.10
  visual_app_builder_shared:
    path: ../shared

dev_dependencies:
  test: ^1.24.0
  http: ^1.2.0
